services:
  mysql:
    image: mysql:latest
    container_name: mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: ${PASSWD}
      MYSQL_DATABASE: ${DATABASE}
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "${PORT1}:${PORT}"

volumes:
  mysql_data:
